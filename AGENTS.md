# AGENTS.md

## 项目定位

本仓库是基于 Unreal Engine 5.4 的 C++ 多人合作 PvE 战斗 Demo，用于实践精简版 Lyra 架构、Gameplay Ability System（GAS）、多人复制、服务器权威、射击、主动技能、敌人 AI、波次关卡和计分。

项目保留 Lyra 中适合学习和复用的核心思想，但不照搬其完整框架。首版以结构清晰、运行可靠、易于理解和可验证为优先目标。

## 核心玩法范围

首个可玩版本应包含：

- 支持 Listen Server 和 Dedicated Server，至少允许两名玩家参与同一场 PvE 战斗。
- 普通射击、换弹、闪避和主动技能通过 Gameplay Ability 实现。
- `GA_Dodge` 使用 Local Predicted，并通过持续约 0.3 秒的 GameplayEffect 授予 `State.Invulnerable`。
- 伤害、死亡、敌人 AI、敌人生成、波次推进和计分由服务器裁决。
- 敌人能够发现、追击、攻击玩家，并能被玩家击杀。
- 包含简单的波次关卡管理、个人分数、团队分数和基础 HUD。
- 至少实现一项与普通射击有明显区别、具有 Cost 或 Cooldown 的主动技能。

## 回答与协作

- 默认使用中文回答，只提供与当前问题直接相关的内容。
- 涉及代码行为、运行流程或实现细节时，先核对实际源代码和配置，再给出结论。
- 解释代码时优先说明运行链路、类职责、网络角色、配置资产和 Blueprint 接入点，不要只做逐行翻译。
- 存在会显著影响结论或实现方式的关键歧义时，先向用户确认。
- 需求明确或可以安全作出合理假设时直接继续，并明确说明必要假设。
- 多步骤任务开始前给出简短计划，并为每一步定义可验证的完成条件。
- 回答保持精炼、准确；必要时补充少量针对性的学习或工程建议，不做无关扩展。
- 用户要求解释、审查或诊断时，不擅自扩大为代码修改；用户明确要求实现时，完成修改并进行与风险相称的验证。

## 语言与注释

- 项目文档、规则说明、用户操作步骤和源码注释默认使用中文。
- API、类型名、类名、函数名、变量名、模块名、资产名、GameplayTag 和 Unreal Engine 专有关键词保留英文，不做生硬翻译。
- 可以直接使用 `Experience`、`GameFeature`、`Gameplay Ability`、`AbilitySystemComponent`、`PlayerState`、`GameState`、`WorldSettings`、`Authority`、`Owning Client` 和 `Simulated Proxy` 等关键词。
- 源码注释优先解释设计原因、生命周期、网络权威和非显然约束，不重复描述代码表面行为。
- 注释保持简洁，不为自解释代码添加无意义注释，也不逐行注释简单语句。
- 同一系统内的 Log 和术语保持一致；Log 可以使用英文或中英混合，但不得随意更换同一概念的名称。

## 编码与修改原则

- 实现前先确认需求、现有结构、调用链和网络职责，不隐藏不确定性。


